## 当前工作进展
- 目前正在应用单窗口化运行的基础上调整显示位置以及处理空白处的黑色背景问题
  - 根据包名过滤可实现launcher全屏运行，应用半屏运行
     private static int getRootMeasureSpec(int windowSize, int rootDimension) {
         int measureSpec;
+         Log.v(TAG, "chenpeng----" + mSContext.getBasePackageName());
+        boolean isLauncher = mSContext.getBasePackageName().equals("com.android.launcher");
+        boolean isAndroid = mSContext.getBasePackageName().equals("android");
+
         switch (rootDimension) {
 
         case ViewGroup.LayoutParams.MATCH_PARENT:
             // Window can't resize. Force root view to be windowSize.
-            measureSpec = MeasureSpec.makeMeasureSpec(windowSize, MeasureSpec.EXACTLY);
+            measureSpec = MeasureSpec.makeMeasureSpec(isLauncher || isAndroid? windowSize : windowSize / 2, MeasureSpec.EXACTLY);
             break;
         case ViewGroup.LayoutParams.WRAP_CONTENT:
             // Window can resize. Set max size for root view.
-            measureSpec = MeasureSpec.makeMeasureSpec(windowSize, MeasureSpec.AT_MOST);
+            measureSpec = MeasureSpec.makeMeasureSpec(isLauncher || isAndroid? windowSize : windowSize / 2, MeasureSpec.AT_MOST);
             break;
